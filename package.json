{
  "name": "draw-cone",
  "version": "1.0.0",
  "description": "A cone drawing app",
  "main": "server.ts",
  "type": "module",
  "scripts": {
    "build": "npm run build --prefix client",
    "postbuild": "rm -rf client/node_modules",
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "cd client && npm run dev"
  },
  "author": "Elena Erganyan",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  }
}
